# 上线就绪整改计划

日期：2026-07-12  
状态：活跃 / 当前唯一执行计划

## 1. 文档职责与优先级

本文件是当前唯一可直接派发开发、测试和发布工作的文档。

- `企业级部门热插拔实施指南(最终蓝图).md`：只定义长期方向和最终边界。
- 本文件：定义当前整改范围、执行顺序、验收标准和冻结范围。
- `docs/archive/`：只保留历史决策、第一阶段工程契约和完成证据，不直接产生新任务。

当长期蓝图、历史文档与本文件发生冲突时，以本文件为准。代码改动涉及主链对象、状态机或接口时，必须先核对归档工程规格与现有测试，再更新本文件中的契约摘要。

## 2. 当前判断

当前产品已跑通受控试点所需的核心工作流：

`老板指令 -> product -> operations -> risk -> 审批 / 返工 / 补件 / 交接`

当前目标不是继续增加部门、页面或适配器，而是把已存在的主链变成可复制交付的客户产品。

当前不具备正式企业级 GA 的主要原因：

1. 发布清单和固定跨仓基线已经可重建，`main` 发布相关路径的受控自动门禁已首次全绿；PR 信任边界和重复绿灯证据仍未闭环。
2. 统一 release gate 已同时取得手工与 `main` push 自动触发的完整绿灯；当前仍未启用 `pull_request`，也尚未形成多个独立发布变更连续通过的证据。
3. Windows 正式安装、升级、失败检测和回滚链路已通过干净 runner，当前仍缺正式企业代码签名及签名候选包复验。
4. PostgreSQL 生产契约已进入统一门禁，但真实供应商模型调用仍未成为持续发布门禁。
5. 后端仍是单活动 API 进程，长任务和 API 请求尚未形成可靠运行底座。
6. 企业客户日常运营所需的账号生命周期、通知、诊断和管理员自助能力不完整。

## 3. 当前范围与冻结规则

### 3.1 允许推进

只允许推进以下四类整改：

1. 发布基线、版本、CI/CD 与可重复构建。
2. 客户端安装、升级、回滚、首启检查和诊断。
3. PostgreSQL、真实模型和隔离环境中的生产路径验证。
4. 任务运行、恢复、监控、账号运营等客户稳定使用能力。

### 3.2 明确冻结

以下内容不得作为当前开发任务，除非本文件更新并完成范围评审：

- 销售工作台及其线索、商机、报价、活动链路扩展。
- 增长工作台、投放、营销素材和渠道自动化扩展。
- 新部门、新 Agent 角色或新业务主链。
- MCP、Browser runner、GUI 自动化和复杂新适配器。
- 为视觉效果进行的前端重绘或大规模组件重构。
- 为“代码更漂亮”进行的无业务价值重构。
- 新计费、支付和订阅功能。

### 3.3 主链工程契约摘要

当前默认主链只能是：

`product -> operations -> risk`

必须继续保持的行为：

- 老板创建、预览、批准并派发业务指令。
- 部门工作项、成果、审批、退回、补件和交接均可追踪。
- `operations` 的已批准成果才可释放 `risk` 审查。
- 老板驳回、补件、返工和失败恢复不能打散原有任务关系。
- CLI/代码执行必须保留任务包、结果包、运行记录和失败原因。
- `sales / growth` 仅可作为冻结兼容入口，不参与默认进度、默认验收或当前整改范围。

### 3.4 发布身份契约

每次可交付发布必须以根目录 `release-manifest.json` 的 `release_id` 为唯一客户、支持和回滚身份。该清单必须同时绑定：根项目源码基线 commit、两个子模块 commit、API 运行版本、数据库 migration head、桌面端 package 版本和默认 release gate。验证器会输出清单实际所在的根项目 commit；不在清单中自引用该 commit。

发布前必须执行：

```powershell
node scripts/verify-release-manifest.mjs
```

不得仅凭 API、Electron 或数据库中的任一单独版本号判断客户运行的发布版本。

## 4. 执行规则

每一个整改项开始前必须回答：

1. 它是否直接解决本文件第 2 节中的一个正式上线缺口？
2. 它是否会改变主链对象、状态机、公共接口或数据库？
3. 是否已有最小验证命令；若没有，能否在本项中补齐？
4. 是否会误把冻结模块重新带入默认入口或验收？

若第 1 题为否，或第 4 题为是，不应开始编码。若第 2 题为是，必须先说明影响范围并保持最小可回滚修改。

## 5. R0：发布基线可复现

### 目标

让任意开发者从一个确定的提交，在干净机器上构建、测试并得到一致的发布物。

### 工作项

1. 清点并处理 `workplace-api`、`workplace-desktop-electron` 的未提交和未跟踪文件。
2. 确保所有实际使用的迁移、脚本、服务和配置模板进入版本管理。
3. 统一 API、桌面端、数据库迁移和发布标签的版本映射。
4. 修复当前失败的后端 smoke 与桌面端 release gate。
5. 移除 release gate 对固定公网、状态不可控环境的硬依赖；改为可重复的隔离验证环境。
6. 在干净 checkout 中验证后端、桌面端、打包后应用和第一阶段 UI 链路。

### 完成标准

- 两个子模块工作树可明确对应发布 commit。
- 干净 checkout 可完成依赖安装、数据库初始化、构建和测试。
- CI 全绿且不依赖人为维护的远端脏环境。
- 发布物可关联到唯一的客户端版本、API 版本、迁移版本和 commit。

## 6. R1：客户可安装、可升级

### 目标

让非研发实施人员可以安装、配置、升级、卸载并收集基础诊断信息。

### 工作项

1. 修复 Windows 安装器构建及其依赖准备流程。
2. 建立标准安装、卸载、升级和失败恢复流程。
3. 定义客户端与服务端版本兼容规则。
4. 在首启流程提供后端连接、团队访问和模型状态检查。
5. 提供面向支持人员的最小诊断信息导出：版本、运行环境、连接状态、任务关联标识和错误摘要。
6. 建立客户端回滚方案，并实际演练至少一次升级失败恢复。

### 完成标准

- Windows 正式安装器可在干净机器构建并安装。
- 客户端可明确提示不兼容版本和连接异常。
- 更新失败不会要求客户手工删除未知目录才能恢复。
- 实施人员仅依据正式文档即可完成首次交付。

## 7. R2：生产路径真实验证

### 目标

让发布门禁验证真实客户路径，而不是仅验证 SQLite、fallback 模板或开发机状态。

### 工作项

1. 为 CI 增加 PostgreSQL 服务，并从零执行迁移和主链回归。
2. 建立 SQLite 与 PostgreSQL 的明确测试边界；生产契约必须在 PostgreSQL 验证。
3. 建立最小真实模型 E2E：连接、超时、失败、限流、格式异常和恢复。
4. 在 UI 和审计中明确区分真实模型结果与 fallback 结果。
5. 构建隔离临时环境，运行桌面端到后端的主链 E2E 和发布后 smoke。
6. 验证一次旧版本升级到新版本的数据库迁移和客户端兼容路径。

### 完成标准

- PostgreSQL 主链测试进入持续 CI。
- release gate 包含真实模型最小 E2E，或在模型依赖不可用时明确失败而非静默 fallback 成功。
- 临时隔离环境中的客户端、服务端和迁移链路可重复通过。
- 生产数据库和模型问题能够在发布前暴露。

## 8. R3：稳定运行与客户运营闭环

### 目标

将当前单实例受控试点系统推进为可稳定运行、可诊断、可恢复的客户产品底座。

### 工作项

1. 将长任务执行从 API 请求路径中分离，形成可持久化的任务运行边界。
2. 补齐任务幂等、领取、超时、失败恢复、重复执行防护和死任务处理。
3. 在具备持久化协调后，再评估多实例、滚动发布和水平扩容；禁止仅增加 worker 数量伪装为高可用。
4. 提供基础运行状态、任务时间线、异常关联标识、监控和告警。
5. 补齐客户运营的最小闭环：邀请、找回密码、离职停用、待审批提醒和管理员基础自助处理。
   - 已完成最小子项：老板可在客户端设置中查看员工、创建可登录员工账号、停用及恢复员工；同团队重复邮箱不会制造额外成员或幽灵席位，停用后员工无法进入当前团队，恢复后可重新进入；正式邀请上线前，已有平台账号不能被其他团队静默加入；可有多人的 `manager` 角色在客户界面明确称为“部门管理员”，不再冒充唯一正式负责人。
   - 仍未完成：正式邮件邀请与邀请 token、忘记密码、自助密码重置、完整权限维护、唯一正式负责人的任命/替换/停用恢复、待审批提醒和更完整的管理员自助闭环；因此本工作项与 R3 整体均不得标记完成。

### 完成标准

- API 重启、任务失败和长任务中断有明确且可验证的恢复语义。
- 客户和支持人员能够定位任务卡在哪一步，并知道下一步恢复动作。
- 系统不依赖原开发者人工盯守才能完成日常运行。
- 在 R3 验收前，不对外承诺多实例高可用或企业级 SLA。

## 9. 当前推荐验收

当前已有验证命令可作为回归基础；具体执行环境和前置条件以各子项目脚本为准：

```powershell
# 后端
cd workplace-api
pytest -q

# 桌面端
cd ..\workplace-desktop-electron
npm run build
npm run test:phase1-ui
npm run electron:build:dir
npm run test:package
npm run test:packaged-app
```

R0 之前不得把已有通过结果视为正式发布通过：当前仍需先修复 release gate、安装器和生产路径验证。

## 10. 阶段出口与后续方向

- R0 未完成：不增加业务功能，不对外形成正式发布承诺。
- R1 未完成：不进行可复制客户交付。
- R2 未完成：不将真实 AI、生产数据库和正式发布能力对外承诺为已验证。
- R3 未完成：不宣称高可用、企业级 SLA 或无人陪同的大规模部署能力。

只有 R0 至 R3 全部完成后，才回到长期蓝图恢复以下方向：

1. 简化销售闭环。
2. 增长和复杂适配器。
3. 新部门热插拔。
4. MCP、Browser runner、GUI 自动化。
5. 更完整的企业化和商业化能力。

## 11. 变更记录

| 日期 | 结论 |
| --- | --- |
| 2026-07-12 | 建立为当前唯一执行计划；历史第一阶段文档转入 `docs/archive/`。 |

## 12. 风险与证据基线

本节记录当前计划为何存在。它不是完成台账；每项只有在代码、构建或运行证据完整通过后，才能在对应 R0-R3 工作项中关闭。

| 优先级 | 风险事实 | 当前证据 / 影响 | 对应整改项 | 关闭证据 |
| --- | --- | --- | --- | --- |
| P0 | 发布版本不可复现 | 2026-07-12 盘点中，`workplace-api` 与 `workplace-desktop-electron` 均有大量 modified / untracked 文件，实际可运行状态无法由当前 HEAD 直接重建。 | R0.1-R0.3 | 干净 checkout 从唯一 commit 重建并通过规定验证。 |
| P0 | 自动持续发布门禁尚未闭环 | 手工 `release-readiness-gate #7` 与由 `main` push 自动触发的 `#8` 均已在隔离 runner 完整成功，受控主分支发布路径已进入自动门禁；但 `pull_request` 仍未启用，私有仓库 PR 凭证策略尚未确定，也没有多个独立发布变更连续绿灯证据。 | R0.4-R0.5 | 在不向不受信任代码暴露 App 私钥的前提下补齐 PR 信任边界，并由多个独立发布提交重复通过完整统一门禁。 |
| P0（已关闭） | 统一跨仓发布基线缺失 | 原后端 PostgreSQL workflow 与桌面端 Windows workflow 只能分别证明各自仓库，无法在同一固定发布身份下串联两个私有子仓库、PostgreSQL、当前 Desktop、固定旧版 Desktop 与 Windows 安装器。 | R0.4-R0.6、R2.5 | 关闭证据：Root `2e7055d`；GitHub Actions `release-readiness-gate #7`（run `29218981517`）成功，总耗时 12 分 49 秒。Linux job `86720285115`（6 分 11 秒）以 GitHub App 短期只读令牌检出固定 Root/API/Desktop 基线，通过发布身份、API 全量回归、PostgreSQL 16 从零迁移、deployment smoke、当前 Desktop 第一阶段 UI 与固定旧版 Desktop 兼容 UI；诊断构件 `8267609678`，digest `sha256:66e1b5b8276039cd7873cad9210263812a669f35a852b5d8674f07fd7c0b9251`。Windows job `86720906007`（6 分 29 秒）通过正式解包应用、固定旧版安装器、当前安装器及完整生命周期，并上传安装器构件 `8267679107`，digest `sha256:d95de94e6e4269c32121263bebd00d99acd7b86c23784a607d325fa65417429c`。 |
| P0 | 安装器正式签名尚未完成 | 固定 `2.0.0` 到 `2.0.1` 的覆盖升级、客户状态保留、故障注入、失败检测、原位回滚、回滚后状态保留和卸载演练，已在本机及干净 Windows runner 通过；当前安装器仍未使用正式企业代码签名。 | R1.2-R1.6 | 使用正式签名候选包在干净 Windows runner 复跑安装、升级、失败恢复、回滚、数据保留和兼容性演练。 |
| P0 | 真实 AI 价值未进入门禁 | 现有 smoke 可使用 fallback 成功，不能证明真实模型连接、超时、限流和结构异常都可控。 | R2.3-R2.4 | 真实模型最小 E2E 纳入 release gate，fallback 明确可见。 |
| P0 | PostgreSQL 生产契约缺少持续证据 | 生产目标为 PostgreSQL，但现有大量验证基于 SQLite；两者差异不能由 SQLite 绿灯覆盖。 | R2.1-R2.2 | PostgreSQL 服务、迁移和主链回归进入 CI。 |
| P1（已关闭） | Desktop revision 迁移覆盖客户已保存配置 | 原实现只要 `uiRevision` 或 `runtimeRevision` 变化，就强制把客户主题改回 `light`，并用安装包默认值覆盖已保存的后端地址、团队和账号模式；企业客户升级后可能无提示连接到错误服务器。原升级 smoke 因新旧版本 revision 相同，未覆盖该故障。 | R1.2-R1.6 / 升级与客户状态保留 | 关闭证据：Desktop `8668e57`；revision 标记继续更新，但默认值只填充缺失项，不覆盖客户已有配置；升级 smoke 先写入旧 revision 和客户自定义后端地址/深色主题，修复前稳定复现 `backendUrl` 被改为 `http://124.71.229.151`、`theme` 被改为 `light`，修复后完整通过 `2.0.0 -> 2.0.1` 安装、启动、故障注入、失败检测、原位回滚、回滚后数据保留及卸载。本地构建与回归已通过；GitHub Actions `desktop-release-smoke #14`（run `29215232688`，job `86709725571`）成功，总耗时 5 分 19 秒、job 耗时 5 分 15 秒，构件 `8266379969`，digest `sha256:cb942e8b8cd936f56f2aef302495ee6ce00011024330e4ba954fb8175329e8b6`。 |
| P0（已关闭） | 异步执行客户文案被编码破坏 | `238bc7f` 将 code-agent / external 的默认任务标题、完成事件、成果标题和老板审查事件摘要共 7 处中文写成连续问号；客户成功执行后会看到乱码，且原测试只验证状态和输出，未验证客户可见成果与事件。 | R3.1-R3.2 / 主链回归 | 关闭证据：API `c65c3fc`；恢复 7 处既有中文，新增源码字符完整性测试并强化真实异步执行成果/事件断言；专项 `9 passed`，全量 `300 passed, 2 skipped`。 |
| P1（已关闭） | API / MCP 中断运行无法在启动时恢复 | external 异步真实执行覆盖 API、MCP、CLI，但原启动恢复仅查询 `adapter_type='cli'`；API 或 MCP 执行期间进程异常退出后，运行会永久停留在 `running`，客户只能持续看到“后台处理中”，既无失败结算也无法进入 replay。 | R3.1-R3.2 / 中断恢复 | 关闭证据：API `136d6c9`；启动恢复现按原子状态条件保守结算全部已领取 `running` 运行，统一记录副作用未知、禁止自动重放，并覆盖 CLI / API / MCP 契约；R3 组合专项 `17 passed`，API 全量 `303 passed, 2 skipped`。 |
| P1（已关闭） | Desktop 有界轮询可被单次网络黑洞无限阻塞 | 客户端虽设置总轮询截止时间，但原状态查询未传入 `AbortSignal`；连接若既不成功也不快速失败，单次 `fetch` 可永久挂起，界面无法进入“状态暂未确认、稍后刷新、不要重复执行”的既有保护分支。 | R3.2 / 客户端失败恢复 | 关闭证据：Desktop `b75538f`；每次状态查询默认最多 5 秒且不超过剩余总截止时间，网络黑洞中止后继续保守确认；真实行为测试验证 700ms 总截止内完成 2 次中止尝试并返回 `timeout`，`npm run test:smoke` 与 `git diff --check` 通过；GitHub Actions `desktop-release-smoke #10`（run `29196192577`，job `86659469145`）成功，耗时 6 分 16 秒。 |
| P1（已关闭） | Desktop 刷新后运行恢复缺少客户证据，失败卡缺少完成时间 | 原 UI smoke 只覆盖失败、切换账号、replay 和审批，不验证真实 `running` 运行在 Renderer 刷新后能否找回；同时服务端已有 `completed_at`，失败恢复卡却不展示，客户排障时无法判断失败发生时间。 | R3.2 / 客户端恢复与诊断 | 关闭证据：Desktop `0c51643`；Operations 隔离真实进程执行受理后刷新 Renderer，恢复同一 `run_id` 的 `running` 卡并验证关联标识、执行阶段、时间线和禁止重复执行提示，完成后再次刷新可见结果包；失败卡展示既有 `completed_at`。`npm run test:smoke`、`npm run test:phase1-ui`、`npm run electron:build:dir`、`npm run test:packaged-app` 与 `git diff --check` 通过；GitHub Actions `desktop-release-smoke #11`（run `29197444870`）成功，耗时 5 分 25 秒。 |
| P1（已关闭） | 正式安装包业务恢复门禁缺失 | 原门禁只验证正式构建可启动及安装、启动、卸载，不能证明客户关闭整个 Electron 进程并重启正式 exe 后还能恢复同一运行，也不能证明安装后客户端、覆盖升级和失败回滚保留客户状态。 | R1.2-R1.6、R3.1-R3.2 | 关闭证据：Desktop `6854d11` 完成本地正式解包与安装后业务恢复；Root `2e7055d` 的 GitHub Actions `release-readiness-gate #7`（run `29218981517`，Windows job `86720906007`，6 分 29 秒）在干净 Windows runner 真实通过正式解包应用启动、安装后业务恢复、`2.0.0 -> 2.0.1` 覆盖升级、客户数据升级后保留、升级后故障检测、原位回滚、回滚应用启动、回滚后客户数据保留和卸载。日志结构化结果确认 `installed_app_started=true`、`installed_business_recovery_verified=true`、`upgrade_started=true`、`customer_data_preserved_after_upgrade=true`、`post_upgrade_failure_detected=true`、`rollback_started=true`、`rolled_back_app_started=true`、`customer_data_preserved_after_rollback=true`、`installed_files_removed=true`、`user_data_preserved_after_uninstall=true`；安装器构件 `8267679107`，digest `sha256:d95de94e6e4269c32121263bebd00d99acd7b86c23784a607d325fa65417429c`。该证据不代表正式代码签名、API 进程退出后续跑、独立 worker、持久任务或高可用。 |
| P1（已关闭） | Desktop 账号切换、权限动作与隐藏运行恢复边界不完整 | 老板切换到员工账号时可能继承上一账号的团队工作区；普通员工可看到老板专属拍板按钮并在点击后收到 403；同一账号启动恢复若被当成身份切换，会废弃合法工作台请求；窗口隐藏期间服务端运行已完成，客户端仍可能长期显示执行中。 | R3.4-R3.5 / 客户账号、权限与正式客户端恢复边界 | 关闭证据：Desktop `58b5bba`；仅在成员、团队或账号模式真实变化时清理旧状态，同身份恢复保留合法请求；老板决策仅向老板开放，风控成果审核按有效 `can_review_artifacts` 权限开放；存在真实 `running` 运行时隐藏窗口保持 30 秒低频刷新。phase1 UI、smoke、正式构建启动及正式解包业务恢复通过；包含该提交的 Root `43a4672` 已由自动 `release-readiness-gate #9`（run `29222387680`）在 Linux 与 Windows 双平台完整通过。 |
| P1（已关闭） | 已注册账号可被其他团队静默加入 | 老板输入任意已注册邮箱后，原接口不需要账号本人知情或接受，便直接创建跨团队成员关系并占用席位；员工只会在下次登录时突然看到新团队。正式邀请尚未实现时，这会把“填写邮箱”错误地当成“本人同意加入”。 | R3.5 / 邀请与身份归属 | 关闭证据：API `2fb0b87` 在同团队重复判断后拒绝跨团队已有账号，不新增成员、不占席位且不改变原账号团队；Desktop `61c5d2e` 给出中文可执行说明。失败测试先稳定复现原接口返回 200，修复后成员业务文件 `43 passed`、API 全量 `305 passed, 2 skipped`，第一阶段 UI、正式构建、正式解包启动及业务恢复全部通过；固定该基线的 Root `fe239ff` 已由自动 `release-readiness-gate #10`（run `29225114364`）在 Linux 与 Windows 双平台完整通过。 |
| P1（已关闭） | 多人权限管理员被误称为唯一部门负责人 | 客户端把 `team_members.role='manager'` 显示为“部门负责人”，但普通成员创建接口不会写入 `departments.owner_member_id`；老板看板和责任路由仍以该独立字段识别唯一负责人。客户会误以为创建了正式负责人，实际只创建了带审核和任务权限的管理员。 | R3.5 / 角色语义与管理员自助 | 关闭证据：经产品决策明确 `manager` 是可有多人的“部门管理员”，唯一正式负责人继续由独立字段和后续任命流程承担。Desktop `10aab70` 修正设置列表、角色选项、权限说明和失败恢复联系人；真实 UI smoke 先以旧称稳定失败，修复后创建 `manager` 必须显示“部门管理员”且不得出现“部门负责人”。`npm run build`、`npm run test:smoke`、`npm run test:phase1-ui`、正式构建、正式解包启动及业务恢复全部通过；固定该基线的 Root `b3317ae` 已由自动 `release-readiness-gate #11`（run `29226937249`）在 Linux 与 Windows 双平台完整通过。 |

| P1 | 单实例限制阻断可靠扩容 | 当前 adapter 运行协调存在单活动 API 进程边界，不能把增加 worker 数量当作高可用。 | R3.1-R3.3 | 任务持久化协调、恢复和多实例前置条件经演练验证。 |
| P1 | 客户升级与故障自助能力不足 | 当前缺少标准化自动更新、回滚、首启自检和诊断导出闭环。 | R1.2-R1.6 | 安装、升级失败恢复、版本提示和诊断导出完成演练。 |
| P1 | 企业日常账号运营不完整 | 现有注册登录和团队入口不等于邀请、找回密码、离职停用、提醒和管理员自助闭环。 | R3.4-R3.5 | 对应客户生命周期流程具有 UI、接口和回归证据。 |
| P2 | 维护边界过大 | `dispatch.py`、`department_workspaces.py`、`SalesObjectsPanels.tsx` 等巨型模块提升回归和交接成本；当前不得以大重构方式处理。 | R0-R3 回归约束 | 仅在主链稳定后，按契约测试和最小可回滚切片逐步拆分。 |
| P2（已关闭） | 框架生命周期 API 已弃用 | API 已从 FastAPI `on_event("startup"/"shutdown")` 等价迁移到单一 lifespan；schema fail-closed、启动恢复、结构化日志和正常关闭语义均由专项测试固定，未改公共接口、数据库或依赖。 | R0 技术债清点 / R2 兼容验证 | 关闭证据：API `db47485`；专项 `3 passed`、全量 `291 passed, 2 skipped`；PostgreSQL 16 + Uvicorn + deployment smoke 通过；`backend-smoke #9` run `29191412503` / job `86646596895`，3m48s 成功且无 `on_event` 弃用告警。 |
| P2（已关闭） | GitHub Actions 依赖版本进入兼容过渡期 | API workflow 已将 `actions/checkout@v4`、`actions/setup-python@v5` 分别升级至 `v7`、`v6`，未改变触发器、权限或验证步骤；`backend-smoke #8` 完整成功且不再产生 Node.js 20 Action 弃用注解。 | R0.4-R0.5 | 关闭证据：API `be10930`，run `29190180762`，完整 smoke 3m26s 成功。 |

### 12.1 当前 R0 实施记录

- 2026-07-12：已按后端 -> 桌面端 -> 根项目顺序推送基线：`workplace-api` 为 `d009cb8`，`workplace-desktop-electron` 为 `0404528`，根项目为 `0bf880f`。在全新临时目录递归 clone 后，根项目及两个子模块均检出上述 SHA 且工作树干净。此记录关闭“远端仓库缺少当前基线提交”的证据缺口，但 R0 仍未完成；版本映射、CI 和安装器链路继续未关闭。

- 2026-07-12：已确认两个主项目均存在大量未提交实现。该状态本身不是“可关闭问题”；下一步必须先将改动分类为待纳入发布基线、临时产物、历史残留或待放弃实验，再决定如何形成正式 commit。
- 2026-07-12：禁止通过删除未跟踪文件、重置工作树或强制覆盖远端的方式伪造“干净基线”。
- 2026-07-12：已完成改动分类、全量后端回归、桌面端构建、第一阶段 UI 主链和解包应用验证；临时 `.tmp/`、`.tmp-*.txt` 已加入后端忽略规则但未删除。已建立仅本地基线提交：`workplace-api` 为 `d009cb8`，`workplace-desktop-electron` 为 `423c481`；两个子模块提交后工作树均为空。此记录不代表 R0 完成，干净 checkout、版本映射、CI 与 release gate 仍未关闭。
- 2026-07-12：桌面端在本机重新执行 `npm run test:release -- --prefix desktop-r0-ci`、解包构建与启动 smoke、NSIS 构建、安装器静态 smoke 以及新增的安装 -> 启动 -> 静默卸载生命周期 smoke，全部通过。生命周期结果明确记录 `installed_app_started: true` 与 `installed_files_removed: true`；该证据只覆盖首次安装生命周期，不覆盖升级、回滚、签名或客户数据保留。
- 2026-07-12：首次桌面端远程流水线 `desktop-release-smoke #1`（run `29181816447`，Desktop `1fad85b`）因默认 `GITHUB_TOKEN` 无权读取独立私有 API 仓库而失败。完整 phase-one UI release gate 不能在缺少受管跨仓库读取身份的 Desktop 仓库中伪装执行；必须配置最小权限凭证，或建立能够读取固定 API 发布基线的统一发布 workflow。
- 2026-07-12：收窄为仓库本身可证明范围后的 `desktop-release-smoke #2`（run `29181911741`，Desktop `b6a6476`）中，NSIS 安装、启动、卸载验证实际通过，但 Electron Builder 非预期尝试发布且测试空目录仍被进程短暂占用，最终流水线失败。随后通过显式 `--publish never` 并移除不属于验收条件的空目录强制清理完成最小修复。
- 2026-07-12：提交 `7dda20c` 对应的 `desktop-release-smoke #3`（run `29182197108`，job `86621850701`）成功。干净 Windows runner 完成依赖安装、固定 NSIS 工具准备、桌面构建 smoke、解包应用构建与启动、正式 NSIS 构建、安装 -> 启动 -> 卸载生命周期 smoke，并上传 `soloops-console-windows-installer` 构件（digest `sha256:3552a63e16f1d3645256a060a9417f434d9deed59da1c62b4d1dfa032d90a9e8`）。这关闭“桌面端没有独立 Windows 安装器持续构建证据”的缺口，但不关闭跨私有仓库完整 release gate。
- 2026-07-12：跨私有仓库读取身份已决策采用 GitHub App。根仓库新增仅支持手工触发的 `release-readiness-gate`：使用 `Contents: Read-only` 的短期安装令牌递归检出固定子模块，先在 Linux/PostgreSQL 16 上执行发布清单校验、后端全量回归、从零迁移、deployment smoke 与 Desktop 第一阶段 UI release gate，再在干净 Windows runner 上执行解包应用启动及 NSIS 安装 -> 启动 -> 卸载生命周期门禁。GitHub App 的最小权限、三仓库安装范围、Client ID/Private Key 配置及证据边界已写入 `docs/release/github-app-release-gate.md`。
- 2026-07-13：统一 workflow 已通过专用 GitHub App 激活。App 仅有 `Contents: Read-only` 与默认 `Metadata: Read-only`，无 webhook、组织或账号权限，且只安装到 `workplace-project`、`workplace-api`、`workplace-desktop-electron`；根仓库已配置 `RELEASE_APP_CLIENT_ID` 与 `RELEASE_APP_PRIVATE_KEY`。首次完整远端运行 `release-readiness-gate #7`（run `29218981517`，Root `2e7055d`）成功。当前 workflow 仍只支持手工触发；在确定私有仓库 PR 凭证策略并验证 `push` / `pull_request` 持续触发前，不把 R0 整体标记完成。
- 2026-07-12：API `be10930` 将 `backend-smoke` 的 `actions/checkout@v4`、`actions/setup-python@v5` 最小升级至 `v7`、`v6`，未改变 workflow 触发器、权限、Python 版本或验证步骤。`backend-smoke #8`（run `29190180762`，job `86643347547`）在干净 Linux/PostgreSQL runner 完整成功，耗时 3m26s，且不再产生 Node.js 20 Action 弃用注解；该证据关闭发布门禁官方 Action 运行时兼容风险，但在 2026-07-12 当时尚不能关闭统一跨仓门禁；后者已由 2026-07-13 的 `release-readiness-gate #7` 补齐。
- 未关闭项：统一跨仓库干净环境门禁和首次完整远端绿灯已经取得，但 R0 仍缺受控的 `push` / `pull_request` 持续触发策略及其重复绿灯证据；R1 的兼容契约、首启连接反馈、脱敏诊断、覆盖升级、失败检测、原位回滚、客户状态保留和正式实施手册已具备本地及干净 Windows runner 回归证据，但仍缺正式代码签名；R2 仍缺“正式安装包 -> API -> PostgreSQL -> 真实供应商模型”的隔离全链证据。因此 R0、R1、R2 均不得标记整体完成。

### 12.2 当前 R1 实施记录

- 2026-07-12：API `a6c2048` 新增独立的 `soloops.desktop-api` 兼容契约，并在 `/health` 与 `/api/runtime/config` 同时公开最小/最大支持版本；deployment smoke 会拒绝契约缺失、两端不一致、名称错误或非法版本范围。API 可观察版本提升为 `1.0.1`，数据库 migration head 保持 `20260712_0031`，完整回归为 `288 passed, 2 skipped`。
- 2026-07-12：Desktop `b69caae` 提升为 `2.0.1`，顶栏连接状态改为基于真实 `/health` 探测，并明确区分检查中、不可达、服务/数据库异常、协议不兼容、待登录、团队连接中及团队访问异常，避免仅凭团队请求结果误报“已连接”。
- 2026-07-12：登录首屏增加平台服务、协议兼容和数据库状态自检；设置页改用未保存的后端地址草稿，候选地址只有在真实健康、数据库就绪且协议兼容后才会生效，修复“用户仅输入尚未保存的地址就立即切断当前业务请求”的客户故障路径。
- 2026-07-12：设置页增加客户端/API 版本、平台、数据库、团队访问和模型状态诊断，并支持导出 schema v1 JSON。导出内容不包含模型 API Key、登录 token、密码或后端地址，错误摘要限制为 240 字符；UI smoke 已验证正常状态、不兼容提示、保存阻断、恢复检查和诊断敏感字段排除。
- 2026-07-12：本机完成 Desktop `npm run build`、第一阶段完整 UI 主链、解包应用构建与启动、`2.0.1` 正式 NSIS 安装器构建、安装器静态校验及安装 -> 启动 -> 静默卸载生命周期。生命周期门禁同时修复了输出目录存在旧版本安装器时的假失败：现在按 `package.json` 当前版本精确选择安装器，旧构件不会掩盖当前版本缺失。
- 2026-07-12：Desktop `23ce1ca` 新增隔离 Electron localStorage 读写助手及 Windows 安装器升级门禁。门禁会安装 `2.0.0`、写入后端地址/团队/账户模式/工作台/主题等测试客户状态、原位升级到 `2.0.1`、验证应用启动和状态保留，再通过移走 `app.asar` 注入真实启动故障，确认验证失败后使用 `2.0.0` 原位回滚，并再次验证启动、状态保留、卸载和程序文件清理。
- 2026-07-12：已从固定 Desktop 提交 `7dda20c047627ac714898f08b18ca9787f000cf5` 创建全新隔离 worktree，使用与该提交 `package-lock.json` 哈希完全匹配的缓存依赖树重新构建 `2.0.0` 安装器；该重建产物通过完整 `2.0.0 -> 2.0.1 -> 故障检测 -> 2.0.0` 门禁。Desktop workflow 与根项目 Windows workflow 均已改为在 runner 中单独 checkout 该固定提交、执行 `npm ci` 并重建旧安装器，不再依赖开发机或 runner 残留构件。
- 2026-07-12：新增 `docs/release/windows-client-install-upgrade-rollback.md`，明确非研发实施人员的交付材料、首次安装、覆盖升级、失败判断、原位回滚、客户状态边界、卸载规则、自动化验收和当前限制；流程明确禁止通过删除 AppData 或未知目录试错恢复。
- 2026-07-12：Desktop `90d7764af416bbe00d24c29e09bb10f157be9850` 对应的 `desktop-release-smoke #7`（run `29187157383`，job `86635255313`）成功。干净 Windows runner 从固定旧提交重建 `2.0.0`，构建当前 `2.0.1`，并通过安装生命周期、覆盖升级、客户状态保留、真实启动故障检测、原位回滚、回滚后状态保留、卸载和确定性临时文件清理；上传构件 `soloops-console-windows-installer`（artifact `8258405081`，digest `sha256:fbcc7734461c21fa9530c7bb58c6a1c225e978fdfd01c40ee81006747f58cd70`）。此前 #5/#6 暴露的 Electron 正常退出竞争、PowerShell `Tee-Object` 中间退出码掩盖及 Windows `rmSync` 假清理/EPERM，已通过等待正常退出、显式检查 `$LASTEXITCODE` 和安全路径内确定性 `unlinkSync`/`rmdirSync` 有界重试关闭。`r0.5` 因 Desktop CI #5 失败，不再作为绿灯发布基线；`r0.6` 将固定该 Desktop 提交。
- 2026-07-13：真实跨 revision 升级审计发现，原 `App.tsx` 在 revision 变化时会无条件覆盖客户已保存的 `backendUrl`、`teamId`、`accountMode` 和 `theme`。强化后的升级 smoke 先写入 `legacy-ui-revision`、`legacy-runtime-revision`、客户自定义后端 `http://127.0.0.1:19999` 与深色主题；修复前确定性失败为后端地址被安装包默认值 `http://124.71.229.151` 覆盖、主题被重置为 `light`。Desktop `8668e57` 保留 revision 标记更新，但只在客户未配置时填充默认值，未修改 API、数据库、公共接口、依赖或目录。`npm run electron:build:installer`、`npm run test:installer:upgrade`、`npm run test:smoke`、`npm run test:phase1-ui`、`npm run test:packaged-app`、两份升级脚本语法检查及 `git diff --check` 已通过；结构化结果确认升级与回滚后客户数据均保留、故障被识别、卸载后 userData 保留。GitHub Actions `desktop-release-smoke #14`（run `29215232688`，job `86709725571`，Desktop `8668e57`）成功，总耗时 5 分 19 秒、job 耗时 5 分 15 秒；构件 `soloops-console-windows-installer`（artifact `8266379969`，digest `sha256:cb942e8b8cd936f56f2aef302495ee6ce00011024330e4ba954fb8175329e8b6`）。该风险的代码、本地证据和 Desktop 仓库远端门禁均已闭环，但不代表安装器已正式签名，也不代替 Root 统一跨仓门禁。
- 未关闭项：R1.2-R1.6 的无签名安装器升级链已获得本机与干净 Windows runner 证据，但安装器仍缺正式 Windows 代码签名；签名候选包形成后必须在干净 Windows runner 复跑同一门禁。因此 R1 整体仍不得标记完成。

### 12.3 当前 R2 实施记录

- 2026-07-12：发现并修复真实模型验收的假阳性：此前 `--use-model` 允许部门草案在模型失败后自动降级为模板成果，部署 smoke 不会断言 `generated_by == model`，因此“模型 smoke 通过”可能仅代表 fallback 通过。该问题已记录为 R2.3-R2.4 的实现证据，但 R2 不因此完成。
- 2026-07-12：`ai_artifacts` 新增并持久化 `generation_source` 与 `generation_fallback_reason`。新生成的部门成果会明确记录 `model`、`fallback` 或历史/人工默认来源；调度事件、命令读模型、团队工作台和销售报价审批返回统一暴露相同来源字段，避免同一成果在不同客户界面呈现出不一致事实。
- 2026-07-12：桌面端研发工程部、财务法务风控部成果卡及员工生成反馈会明确显示“真实模型生成”或“模板兜底生成”；出现兜底时显示机器可读的失败类别，不能再把模板成果表述为模型成果。
- 2026-07-12：部署 smoke 与主链 smoke 增加 `--require-real-model`。该模式要求显式上游 Base URL、模型名称和通过环境变量注入的 API Key；任一产品、运营、风控部门草案不是 `model` 即失败。默认本地 smoke 保持 fallback 验证范围，不会被误称为真实模型 E2E。
- 2026-07-12：已验证模型成功与失败语义：新增 4 个来源契约测试通过；本地 OpenAI-compatible 上游契约演练确认三次真实请求均进入上游，产品、运营、风控三份草案均为 `model`；另一次故意返回失败的演练被门禁以非模型来源拒绝。完整后端回归为 `281 passed, 2 skipped`；桌面端 `npm run build` 和 `npm run test:phase1-ui` 通过。
- 2026-07-12：首次远端真实执行 `backend-smoke #3`（run `29179282520`，API `4d5a86d`）在 `Run full backend regression` 阶段失败，PostgreSQL 迁移和部署 smoke 未被执行。四个失败暴露的不是生产逻辑缺陷，而是测试依赖开发机已安装 Claude/Codex CLI，以及 Linux 测试文件默认 `0644` 与生产私有文件权限契约冲突。测试已改为显式模拟 CLI 可用，并先以 `0600` 构造受保护结果文件；在刻意移除真实 CLI 的 PATH 环境中，原四个失败用例为 `4 passed`，随后全量回归为 `281 passed, 2 skipped`。修复提交为 `ad87a93`。
- 2026-07-12：`backend-smoke #4`（run `29180693121`，API `ad87a93`）成功，远端 Linux 全量回归、从零迁移 PostgreSQL、启动 PostgreSQL-backed API、运行 deployment smoke 均完成。该证据关闭“PostgreSQL workflow 从未真实跑通”的缺口，但不等于真实模型和安装包全链已验证。
- 2026-07-12：真实模型失败矩阵已扩充至上游超时、HTTP 429、HTTP 5xx、HTTP 200 但 JSON 非法、缺少必填成果章节，以及一次 fallback 后下一次生成恢复为 `model`。来源必须分别稳定记录为 `upstream_model_error` 或 `model_response_invalid`，恢复后不得残留 fallback 标记；专项测试为 `10 passed`，完整后端回归为 `287 passed, 2 skipped`。提交 `14bc62e` 对应的 `backend-smoke #5`（run `29181081115`）成功，完整 PostgreSQL 发布验证步骤再次通过。
- 2026-07-12：新增独立 PostgreSQL 数据库升级 smoke，在受保护的空数据库 `workplace_upgrade` 中执行 `20260709_0030 -> 20260712_0031`。演练确认历史 team、department、artifact 数据完整保留，`generation_source` 正确回填，旧版本 INSERT 仍兼容，并通过当前 `assert_schema_ready()`；脚本会拒绝错误数据库名和非空数据库，避免误操作现有客户数据。
- 2026-07-12：固定旧 Desktop `2.0.0` 提交 `7dda20c047627ac714898f08b18ca9787f000cf5`，对当前 API `1.0.1`、PostgreSQL 16 完整运行第一阶段 UI release gate 并通过。验证覆盖主链、adapter 失败与脱敏、员工端恢复/replay、owner approval、运营与风控真实外部进程执行、审批及返工，关闭旧客户端兼容路径缺口。
- 2026-07-12：数据库升级和旧客户端实跑暴露 PostgreSQL 专属缺陷：`adapter_run_logs.started_at/completed_at` 为 `TEXT`，生命周期 SQL 却将其与 `CURRENT_TIMESTAMP` 直接 `COALESCE`，SQLite 会隐式兼容而 PostgreSQL 返回 500。现已在普通 adapter、code-agent、external-run、失败结算和启动恢复路径统一显式转换为文本，未修改数据库结构或公共接口。
- 2026-07-12：PostgreSQL deployment smoke 已扩展覆盖 adapter fail、敏感信息脱敏、问题回答、replay、审批、external-run dry-run、真实进程执行及 `started_at/completed_at` 落库；当前 API 默认占位命令的预期失败语义与旧 Desktop 注入成功命令的成功语义分别获得真实 PostgreSQL 证据。最终后端全量回归为 `288 passed, 2 skipped`。
- 2026-07-12：关闭 FastAPI 生命周期弃用风险。`app/main.py` 迁移到 lifespan 后保持 schema 未就绪时拒绝启动、CLI adapter 中断恢复、启动/关闭结构化日志和 API 契约不变；新增 3 个专项测试固定成功启停与 fail-closed 行为。最终本地全量回归为 `291 passed, 2 skipped`，PostgreSQL 16 升级演练、当前 schema、Uvicorn 健康检查和 `product -> operations -> risk` deployment smoke 全部通过；API `db47485` 对应 `backend-smoke #9`（run `29191412503`，job `86646596895`）3m48s 成功，远端步骤不再出现 FastAPI `on_event` 弃用告警。
- 未关闭项：旧版本数据库升级演练和旧客户端兼容验证已经关闭；真实模型门禁仍未绑定到受管 CI 密钥/发布环境，当前失败矩阵使用受控 OpenAI-compatible 传输模拟而不是真实供应商限流与超时现场；隔离临时环境中的“正式桌面安装包 -> 后端 -> PostgreSQL -> 真实供应商模型”全链 E2E 仍未完成。因此 R2 整体仍不得勾选完成。

### 12.4 当前 R3 实施记录

- 2026-07-12：完成 R3.1-R3.2 的第一条最小垂直切片。code-agent 与 external CLI 真实执行请求新增可选 `wait_for_completion`；默认值保持 `true`，旧客户端继续同步等待。仅在真实执行且显式传入 `false` 时，API 完成原有权限、确认文本、任务包指纹、待补件检查和原子 `planned -> running` claim 后立即返回 `accepted=true`，随后复用现有成功/失败结算路径回写 `adapter_run_logs`、事件、成果和老板决策，不新增数据库表、框架或依赖。
- 2026-07-12：API 新增进程内后台任务强引用 registry，统一消费任务异常；FastAPI lifespan 正常关闭时会取消并等待活动任务。取消继续经过既有 claimed execution guard，将运行记录结算为 `failed`，保留“外部结果未知，核实副作用后再 replay”的恢复语义，避免出现永久 `running` 或未消费任务异常。
- 2026-07-12：Desktop 的 code-agent 与 operations/risk external CLI 真实执行改为异步受理；dry-run 和旧同步响应保持原路径。客户端复用 command adapter-runs 列表接口进行有界轮询，分别以服务端默认执行超时加 30 秒宽限为截止时间。临时断网或代理错误不会直接显示“真实执行失败”；轮询截止后明确提示状态未知、稍后刷新且不要重复执行。异步响应中的执行结果为空时具有显式分支和空值防护。
- 2026-07-12：本切片验证证据为：lifespan/registry 专项 `4 passed`；异步受理、claim、关闭结算与启动恢复相关专项 `13 passed, 217 deselected`；API 全量 `295 passed, 2 skipped`；Desktop `npm run build` 与 `npm run test:smoke` 通过；独立 PostgreSQL 16 临时环境中的 `20260709_0030 -> 20260712_0031` 升级、当前迁移、PostgreSQL-backed Uvicorn 和 deployment smoke 通过。
- 2026-07-12：完成 R3 的第二条最小诊断切片。API 在既有 `adapter_run_log` 响应中新增派生型 `diagnostics`，稳定复用 `adapter_run_id` 作为支持关联标识，并依据现有 `created_at`、`started_at`、`completed_at`、`updated_at` 派生当前阶段、最近变化时间、保守副作用状态、操作指引和紧凑时间线；没有新增数据库结构、迁移、endpoint、依赖或任务基础设施。
- 2026-07-12：真实 HTTP 异步受理响应已验证包含 `correlation_id`、`executing` 阶段、`unknown_requires_verification` 副作用状态、`execution_started` 时间线和“不要重复执行”指引。对已开始后失败或取消的运行，一律要求先核实外部系统副作用，不向客户承诺“安全重试”。
- 2026-07-12：Desktop 共享恢复卡现在覆盖 `running`、`failed`、`waiting_approval`，展示支持关联标识、当前阶段、最近变化、时间线、下一步动作、来源运行和失败原因；Product、Operations、Risk 工作台可展示正在后台执行的最新运行。诊断字段保持可选，连接旧服务端时仍采用保守 fallback，不会因字段缺失崩溃或错误提示可安全重试。
- 2026-07-12：本诊断切片验证证据为：诊断纯函数、异步受理、后台 registry 与关闭结算组合专项 `8 passed`；API 全量 `299 passed, 2 skipped`；Desktop `npm run build` 与 `npm run test:smoke` 通过。
- 2026-07-12：持续审计发现异步执行提交 `238bc7f` 在源码层将 7 处客户可见中文写成连续问号，影响 code-agent / external 的默认任务标题、完成事件、成果标题和老板审查事件摘要。该问题会在真实执行成功后直接污染客户工作台和审计时间线，按 P0 回归优先于新增死任务治理处理。
- 2026-07-12：已恢复全部 7 处既有中文，并新增字符完整性源码契约，同时强化真实异步 HTTP 执行测试，直接验证持久化成果标题、dry-run/real-run 完成事件和老板审查事件摘要。验证证据为组合专项 `9 passed`、API 全量 `300 passed, 2 skipped`。
- 2026-07-12：持续状态机审计发现 external 异步真实执行支持 API、MCP、CLI，但启动恢复仍只处理 CLI。API 或 MCP 执行在进程异常退出后会永久停留在 `running`，客户端持续显示“后台处理中”，且旧状态既不能重新执行也不会生成恢复事件；该缺口属于当前已支持主链的确定性中断恢复错误，而不是未来高可用增强。
- 2026-07-12：API `136d6c9` 将启动恢复泛化为全部已领取的 `running` adapter run，继续采用原子 `running -> failed`、外部副作用未知、禁止自动重放和重新审批后 replay 的既有保守语义；没有新增表、迁移、endpoint、依赖或队列。新增 API / MCP 恢复契约、CLI 恢复 reason code 断言和恢复文案字符完整性防线。验证证据为 R3 组合专项 `17 passed`、API 全量 `303 passed, 2 skipped`。
- 2026-07-12：继续审计 Desktop 后台运行轮询，确认原“有界轮询”只约束轮询循环，未约束单次 `fetch`。在连接黑洞场景下，状态查询可永久不返回，客户会一直看到后台处理中，无法进入既有的状态未知和禁止重复执行提示；该问题属于 R3.2 的确定性客户等待缺陷。
- 2026-07-12：Desktop `b75538f` 为状态查询增加可选 `AbortSignal`，每次查询默认 5 秒上限且受总剩余截止时间约束；单次查询中止仅视为暂时无法确认，不会伪装成真实任务失败。新增无依赖真实行为测试，验证黑洞请求被中止后重试、700ms 总截止严格返回 `timeout`，并验证终态可立即返回；`npm run test:smoke` 与 `git diff --check` 通过；GitHub Actions `desktop-release-smoke #10`（run `29196192577`，job `86659469145`）成功，耗时 6 分 16 秒。未修改 API、数据库、公共响应或任务基础设施。
- 2026-07-12：持续审计 Desktop 恢复证据，确认原 phase1 UI smoke 的失败恢复只覆盖失败卡、账号切换、replay 和老板审批，没有覆盖真实 `running` 运行的 Renderer 刷新；同时失败运行虽已有 `completed_at`，共享恢复卡却没有展示完成时间，客户无法从失败卡判断故障发生时点。
- 2026-07-12：Desktop `0c51643` 在不修改 API、数据库、公共接口或任务基础设施的前提下，补齐失败恢复卡完成时间，并用隔离 Operations 黑窗口真实进程建立稳定的 12 秒运行窗口。phase1 UI smoke 现在验证：异步受理后刷新 Renderer，登录态恢复并找回同一 `run_id` 的 `running` 运行；关联标识、真实执行阶段、执行开始时间线和“不要重复执行”指引完整；后台完成后再次刷新仍能恢复标准结果包。最终 JSON 显式输出 `renderer_reload_recovery_departments_expected/completed`，且 phase1 强制要求 `operations` 完成该门禁。
- 2026-07-12：本轮验证为 `npm run test:smoke`、`npm run test:phase1-ui`、`npm run electron:build:dir`、`npm run test:packaged-app`、两份 Node 脚本语法检查和 `git diff --check` 全部通过；GitHub Actions `desktop-release-smoke #11`（run `29197444870`）成功，耗时 5 分 25 秒。`test:packaged-app` 只验证当前 `file://dist` 正式构建启动和页面非空，不能据此宣称正式安装包业务恢复已验证。
- 2026-07-12：Desktop `6854d11` 在不打包完整 UI Runner、不新增依赖、不修改 API、数据库或公共接口的前提下，新增聚焦的正式客户端业务恢复门禁。门禁使用正式 Electron EXE 和 `file://.../app.asar/dist/index.html`，在隔离 SQLite + Uvicorn 后端上登录 Operations 员工、受理真实执行、确认 `running`，随后退出整个 Electron 进程树并以相同 userData 重启，验证会话恢复、同一 `run_id`、执行阶段、时间线、关联标识和禁止重复执行提示；最终正常运行卡显示 `succeeded`，标准结果包恢复，且没有创建第二条运行。
- 2026-07-12：本轮本地验证通过 `npm run build`、`npm run electron:build:dir`、`npm run test:packaged-app`、`npm run test:packaged-business-recovery`、`npm run test:smoke`、`npm run test:phase1-ui`、`npm run electron:build:installer`、`npm run test:installer`、`npm run test:installer:lifecycle`、`npm run test:installer:upgrade`、三份 Node 脚本语法检查和 `git diff --check`。结构化证据确认 `whole_process_restart=true`、`session_restored=true`、`same_run_id=true`、重启后状态为 `running`、最终状态为 `succeeded`、结果包恢复且 `duplicate_run_created=false`；安装后 lifecycle 同时确认安装应用可启动、业务恢复已验证并完成卸载。
- 2026-07-12：完整业务恢复只接入根统一 `release-readiness-gate`；Desktop 独立 workflow 继续只承担其有权限执行的安装器生命周期验证，避免因无法读取私有 API 仓库产生确定性红灯。根统一 workflow 增加正式解包业务恢复与 `--verify-business-recovery` 安装后门禁；该日尚未配置 GitHub App 凭证，因此当时只能认定“实现与本地证据完成”。此历史缺口已由 2026-07-13 的 `release-readiness-gate #7` 关闭。
- 2026-07-13：Desktop 独立流水线 `desktop-release-smoke #12`（run `29213379318`，job `86704802562`，Desktop `6854d11`）成功；总耗时 5 分 15 秒，job 耗时 5 分 12 秒。该流水线证明 Desktop 仓库自身的正式构建、安装、升级与回滚门禁继续成立，但因其不读取私有 API 仓库，当时仍不能证明完整跨仓正式客户端业务恢复；该证据边界随后由 Root 统一 workflow #7 补齐。
- 2026-07-13：Root `2e7055d55d9f7160c712c66adab551bc1cfefd57` 对应的统一 `release-readiness-gate #7`（run `29218981517`）首次完整成功，总耗时 12 分 49 秒。Linux job `86720285115`（6 分 11 秒）通过 GitHub App 跨私有仓库固定基线检出、发布身份校验、API 全量回归、PostgreSQL 16 从零迁移与 deployment smoke、当前 Desktop 第一阶段 UI、固定旧版 Desktop 对当前 PostgreSQL API 的兼容 UI；诊断构件 `8267609678`，digest `sha256:66e1b5b8276039cd7873cad9210263812a669f35a852b5d8674f07fd7c0b9251`。Windows job `86720906007`（6 分 29 秒）通过 API 运行依赖安装、正式解包应用、安装后业务恢复、固定 `2.0.0` 与当前 `2.0.1` 安装器、覆盖升级、客户数据保留、故障注入与检测、原位回滚、回滚后数据保留和卸载；安装器构件 `8267679107`，digest `sha256:d95de94e6e4269c32121263bebd00d99acd7b86c23784a607d325fa65417429c`。该成功运行对应的是 `2e7055d`，不包括之后仅更新证据文档的提交。
- 2026-07-13：为关闭“只能人工点门禁”的持续性缺口，Root 以最小范围为统一 workflow 增加 `main` 分支发布相关路径触发：仅覆盖 workflow 自身、`.gitmodules`、`release-manifest.json`、`scripts/**` 及两个子模块 gitlink；文档提交不触发 12 分钟级完整门禁。保留 `workflow_dispatch`，暂不增加 `pull_request`，避免在 PR 信任边界未确定前向 PR 代码提供 GitHub App 私钥。`e4bdcbe` 推送后由 `push` 自动创建的 `release-readiness-gate #8`（run `29220005160`）完整成功，总耗时 11 分 19 秒；Linux job `86723116709`（6 分 03 秒）上传诊断构件 `8267907140`，digest `sha256:46e13738034948499d76109bdd2964eeb9176bbef553f73d2b548fd84a681a72`；Windows job `86723712676`（5 分 11 秒）上传 79.3 MB 安装器构件 `8267960570`，digest `sha256:482bc83924493696f8a4fc0d835ecfc0fbf3e6f8d06295545a1b77a63bd66ca1`。页面明确记录 `Triggered via push` 与 `on: push`。该证据关闭“main 发布相关路径自动触发未经验证”，但不等于 PR 门禁、重复发布绿灯或 R0 整体完成。
- 2026-07-13：继续按真实客户账号路径审计，发现切换身份时旧团队状态和仍在途请求未被隔离，员工可能继承老板工作区；Product/Risk 员工页面仍暴露老板专属拍板动作；同一账号启动恢复若无条件清理状态，会废弃已经发出的合法员工工作台请求；窗口隐藏时原轮询全部停用，运行即使已在服务端完成，客户端也可能长期停留在“执行中”。这些问题属于已支持主链中的客户隔离、权限可见性和恢复时序缺陷，不是新增功能。
- 2026-07-13：Desktop `58b5bba` 以最小范围修复上述边界：只有成员、团队或账号模式真实变化时才使旧请求失效并清空团队、命令及员工工作台状态，同身份会话恢复不废弃合法请求；老板决策回调仅在老板模式提供；风控成果审核按所属风控部门的有效 `can_review_artifacts` 权限开放；Product 决策区在无操作权限时不渲染按钮；隐藏窗口仅在存在真实 `running` adapter run 时维持 30 秒低频刷新，获取终态后恢复节流。未修改 API、数据库、公共接口、目录、依赖或任务基础设施。
- 2026-07-13：本轮本地证据通过 `node --check scripts/smoke-sales-ui-runner.cjs`、`npm run build`、`npm run test:phase1-ui`、`npm run test:smoke`、`npm run electron:build:dir`、`npm run test:packaged-app`、`npm run test:packaged-business-recovery` 与 `git diff --check`。phase1 UI 直接断言 Product/Risk 员工看不到“老板通过/老板驳回”；正式解包客户端以相同 userData 重启后恢复同一 `run_id`，重启后状态为 `running`，隐藏期间继续低频刷新，最终恢复 `succeeded` 结果包且 `duplicate_run_created=false`。这只证明客户端进程重启和现有 API 运行的恢复展示，不代表 API 进程退出后续跑、独立 worker、持久任务队列或高可用。
- 2026-07-13：完成 R3.5 的成员账号管理最小子项。API `2e8c255` 阻止同一团队用相同邮箱重复创建第二个成员和幽灵席位，并将席位检查放在重复成员判断之后，保证客户看到真实原因；该切片当时仍允许既有平台账号直接加入其他团队。Desktop `1b4696c` 仅向老板开放员工成员管理，可查看席位与员工、创建带临时密码的可登录员工账号、停用及恢复员工，并且只开放第一阶段 `product -> operations -> risk` 部门，不重新开放已冻结的销售和增长。该版本界面明确说明当前不是邮件邀请，临时密码需通过可信渠道线下交付；当时对已有平台账号仍提示沿用原密码。后续跨团队静默加入风险由下一条身份归属切片纠正。未修改数据库、公共接口、目录或依赖。
- 2026-07-13：本子项验证证据为 API 成员业务文件 `42 passed`、API 全量 `304 passed, 2 skipped`；Desktop `node --check scripts/smoke-sales-ui-runner.cjs`、`npm run build`、`npm run test:phase1-ui`、`npm run electron:build:dir`、`npm run test:package`、`npm run test:packaged-app`、`npm run test:packaged-business-recovery` 全部通过。真实 UI smoke 覆盖老板创建员工、停用后该账号登录但无法进入团队、恢复后重新获得团队、普通员工设置中看不到老板成员管理入口。当前仍没有正式邮件邀请、邀请 token、忘记密码、自助密码重置、完整权限维护和审批提醒闭环，因此 R3.5 与 R3 整体保持未完成。
- 2026-07-13：Root `43a467256a93bd5ab67b9b5db20c60f2027f8cb5` 固定 API `2e8c255` 与 Desktop `1b4696c` 后，由 `push` 自动触发的 `release-readiness-gate #9`（run `29222387680`）完整成功，总耗时 11 分 15 秒。Linux `PostgreSQL and phase-one UI gate` job `86729975671` 用时 5 分 52 秒，诊断构件 `8268669093`，digest `sha256:1824158ef4f6aa525ddd5a57f0675e976bdf55a7a341ef904ceaadb4f783e5dc`；Windows `packaged application and installer gate` job `86730554239` 用时 5 分 17 秒，79.3 MB 安装器构件 `8268727101`，digest `sha256:7ba52163af1555f590812724ed8e496c3a78f583c2c235a66265f0d110fa8adf`。该远端绿灯证明本次固定基线通过现有 PostgreSQL、第一阶段 UI、正式解包应用、业务恢复与安装器门禁，但不扩大 R3.5 的完成边界。
- 2026-07-13：继续审计成员创建链路时确认，老板输入一个已注册但不属于当前团队的邮箱，原 API 会直接复用该全局用户并创建跨团队 membership；账号本人没有邀请、接受或拒绝动作，只会在下次登录时看到新增团队。经产品决策采用临时保护规则：正式邀请接受流程上线前，禁止已有平台账号被其他团队静默加入；新邮箱创建企业员工账号保持不变。该决策必要性充足、不涉及过度开发且直接属于 R3.5，没有用临时 membership 或伪邀请冒充正式邀请。
- 2026-07-13：API `2fb0b87` 在保留“同团队重复邮箱应恢复现有成员”的语义后，对跨团队已有账号返回 409；拒绝不会新增成员、占用席位或改变原账号团队。Desktop `61c5d2e` 将设置页说明改为“已有平台账号不能直接加入，正式邀请上线后必须由本人接受”，并把两类 409 转成中文可执行反馈。回归测试先稳定复现原接口返回 200；修复后专项 `2 passed`、成员业务文件 `43 passed`、API 全量 `305 passed, 2 skipped`，`node --check scripts/smoke-sales-ui-runner.cjs`、`npm run build`、`npm run test:phase1-ui`、`npm run electron:build:dir`、`npm run test:package`、`npm run test:packaged-app`、`npm run test:packaged-business-recovery` 与 `git diff --check` 全部通过。UI smoke 真实注册独立团队账号，验证老板侧拒绝提示、成员列表无新增、原账号仍只属于原团队，并继续验证全新邮箱创建、停用和恢复不受影响。正式邀请、接受/拒绝、邀请 token、过期与撤销仍未实现，因此 R3.5 不得标记完成。
- 2026-07-13：Root `fe239ffd79a642079d4ed28cad8772bb9f7c4fde` 固定 API `2fb0b87` 与 Desktop `61c5d2e` 后，由 `push` 自动触发的 `release-readiness-gate #10`（run `29225114364`）完整成功，总耗时 11 分 23 秒。Linux `PostgreSQL and phase-one UI gate` job `86737528126` 用时 5 分 55 秒，诊断构件 `8269530851`，digest `sha256:46c441d97cbd43c06ce5f424bdbb15c64e225cb1d86d235919ab315a9449a367`；Windows `packaged application and installer gate` job `86738153814` 用时 5 分 23 秒，79.3 MB 安装器构件 `8269591659`，digest `sha256:cae429f25907bded783b9ebe08ddc0f95a93a3553dfbdd038a2e61df3e85ea85`。该远端绿灯关闭本次“已注册账号静默跨团队加入”的固定基线证据缺口，但不代表正式邀请、接受/拒绝、过期、撤销或邮件送达已经实现。
- 2026-07-13：继续审计成员停用/恢复与部门责任数据时确认，客户端把 `team_members.role='manager'` 直接显示为“部门负责人”，但普通成员创建只写角色与权限，不会写入后台用于老板看板和责任路由的唯一负责人字段 `departments.owner_member_id`；停用正式负责人会清空该字段，恢复成员也不会自动重新任命。经产品决策将两层语义明确分开：`manager` 是可有多人的“部门管理员”，唯一正式负责人的任命、替换和停用恢复留给独立流程，不通过改一行字段或自动覆盖现任负责人伪装完成。
- 2026-07-13：Desktop `10aab70` 将成员列表、角色下拉、权限说明和失败恢复联系人统一改为“部门管理员”，并明确该角色不等于唯一正式负责人；未修改 API、数据库、后端 `manager` 枚举、权限行为或公共接口。真实 UI smoke 先创建 `manager` 并因旧界面仍显示“部门负责人”稳定失败，修复后直接断言员工行包含“部门管理员”且不含“部门负责人”。本地 `node --check scripts/smoke-sales-ui-runner.cjs`、`npm run build`、`npm run test:smoke`、`npm run test:phase1-ui`、`npm run electron:build:dir`、`npm run test:package`、`npm run test:packaged-app`、`npm run test:packaged-business-recovery` 与 `git diff --check` 全部通过。唯一正式负责人的任命/替换/停用恢复仍未实现，因此 R3.5 不得标记完成。
- 2026-07-13：Root `b3317ae62b1d888841e2b3917e57127c5188ed7b` 固定 Desktop `10aab70` 后，由 `push` 自动触发的 `release-readiness-gate #11`（run `29226937249`）完整成功，总耗时 11 分 56 秒。Linux `PostgreSQL and phase-one UI gate` job `86742916529` 用时 6 分 16 秒，诊断构件 `8270174754`，digest `sha256:7aa58e9360ee4eb788936c8819c36321f0bce0596e84ae0b1b07f5a2f889c8e6`；Windows `packaged application and installer gate` job `86743695240` 用时 5 分 33 秒，79.3 MB 安装器构件 `8270257157`，digest `sha256:e8f9856245f0f89dbb14239a4fc3715eacb8616e750965d1771a6a11a506bc66`。该远端绿灯关闭“多人管理员被误称唯一负责人”的固定基线证据缺口，但不扩大为唯一正式负责人的任命、替换或停用恢复已经实现。
- 未关闭项：正式解包客户端、安装后客户端业务恢复及统一跨仓首次远端绿灯已由 `release-readiness-gate #7` 关闭，但当前后台执行仍属于单个 API 进程内的 `asyncio.Task`，不是独立 worker、持久任务队列或高可用调度系统；API 进程终止后不会由其他实例接管或自动续跑，只会在关闭/下次启动恢复时按失败未知语义结算。启动时将全部 `running` 运行保守结算为失败的前提是只有一个活动 API 进程；若直接启用多实例或滚动发布，新实例可能误结算其他实例仍在执行的任务。当前关联标识只是稳定运行 ID，不是跨服务 trace；时间线是状态时间戳投影，不是逐步骤事件追踪。死任务自动判定、正式监控告警、多实例协调、滚动发布验证、正式代码签名、真实供应商模型 E2E 和自动化 PR 门禁仍未完成。因此 R3.1、R3.2、R3.3 及 R3 整体均不得标记完成。
